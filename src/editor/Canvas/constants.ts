// Коэффициент мм → px холста — единственный в проекте (архитектурное правило CLAUDE.md).
// Остальное — экранные размеры оверлеев редактора, к модели документа не относятся.

export const BASE_PX_PER_MM = 96 / 25.4; // 100% зума = «настоящий» CSS-пиксель при 96dpi
export const RULER_SIZE = 20; // px, совпадает с --ruler-size
export const PAD_MM = 20; // запас вокруг карточки, чтобы вылет и линейки было видно при любом zoom
export const MAJOR_TICK_MM = 10;
export const MINOR_TICK_MM = 5;

export const HANDLE_SIZE = 7;
// у линии нулевая высота в модели — даём оверлею минимальную толщину хитбокса, иначе некликабельна
export const MIN_HIT_HEIGHT_PX = 8;
// порог в экранных пикселях, не в мм — иначе на 400% zoom примагничивание срабатывало бы
// от одного взгляда, а на 25% не срабатывало бы вовсе
export const SNAP_THRESHOLD_PX = 5;
